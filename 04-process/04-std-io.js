#!usr/bin/node
const msg=['name','email','qq','mobile'];
console.log(msg[0]+':');
#!usr/bin/node

cosnt msg=['name','email','qq','monile'];
var i=0,me=();

process.stdio.on('data',(data)=>{
  var cmd='me'
})


//process.stdio.on('data',(data)=>{

  // cosnole.log(data.slice(0,data.length-1).toString('utf-8'))
  //data date.tostring('utf-8')
    // }
  
